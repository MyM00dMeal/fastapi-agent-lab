FastAPI Calculator Agent

A simple rule-based calculator built using FastAPI.  
It performs basic operations like add, subtract, multiply, and divide without using any AI models.

Features
- Rule-based agent logic
- REST API for calculations
- Simple web UI using HTML

Run
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
